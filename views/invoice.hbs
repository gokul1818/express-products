<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Invoice</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }

    .invoice-box {
      border: 1px solid #ccc;
      padding: 20px;
      max-width: 600px;
      margin: auto;
    }

    h2 {
      text-align: center;
      color: #333;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }

    th,
    td {
      padding: 10px;
      border: 1px solid #ddd;
      vertical-align: middle;
    }

    th:nth-child(1),
    td:nth-child(1),
    td:nth-child(3),
    td:nth-child(4),
    td:nth-child(5),
    th:nth-child(3),
    th:nth-child(4),
    th:nth-child(5) {
      text-align: center;
    }

    .product-info {
      display: flex;
      align-items: center;
    }

    img {
      max-width: 60px;
      height: auto;
      margin-right: 10px;
    }
  </style>
</head>

<body>
  <div class="invoice-box">
    <h2>Invoice</h2>
    <p><strong>Order ID:</strong> {{orderId}}</p>
    <p><strong>Customer Name:</strong> {{userId}}</p>
    <p><strong>Shipping Address:</strong> {{shippingAddress}}</p>
    <p><strong>Order Date:</strong> {{orderDate}}</p>
    <p><strong>Payment Method:</strong> {{paymentMethod}}</p>

    <table>
      <thead>
        <tr>
          <th>S.No</th>
          <th>Product</th>
          <th>Quantity</th>
          <th>Price</th>
          <th>Subtotal</th>
        </tr>
      </thead>
      <tbody>
        {{#each productsDetails}}
        <tr>
          <td>{{inc @index}}</td>
          <td>
            <div class="product-info">
              <img src="{{productImage}}" alt="{{productName}}" />
              <span>{{productName}}</span>
            </div>
          </td>
          <td>{{productQuantity}}</td>
          <td>{{currency productPrice}}</td>
          <td>{{currency (multiply productPrice productQuantity)}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>

    <h3 style="text-align: right;">Total: {{currency totalPrice}}</h3>
  </div>
</body>

</html>